import { FirebaseContext, withFirebase, FirebaseProvider } from './context';
import Firebase from './firebase';

export default Firebase;

export {
  FirebaseContext,
  FirebaseProvider,
  withFirebase
}